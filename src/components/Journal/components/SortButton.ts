import styled from 'styled-components';
import { buttonTextStyle, StyledButton } from '../../Button/styled/StyledButton';

export const SortButton = styled(StyledButton)`
  ${buttonTextStyle};
  padding-left: 0;
`;

import styled from 'styled-components';
import { paleRedColor } from '../../shared/colors';

export const StyledValidationError = styled.span`
  margin-top: 10px;
  color: ${paleRedColor};
  font-size: 15px;
`;
